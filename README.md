# Docker homework Alex Matveenko

***

Main commands:

* `run-script` - run file
* `init-dev` - install requirements
* `doc-i-run` - make all actions needed for run project. (docker)
* `doc-i-purge` - make all actions needed for purge project and related data. (docker)
